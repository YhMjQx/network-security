[TOC]



# ==内网环境配置==

#### 一、内网渗透原理

##### 1、内网穿透

利用各种隧道技术，以网络防火墙允许的协议，绕过网络防火墙的封锁，实现访问被封锁的目标网络。

（1）端口转发：将发往外网某个端口的数据完全转发给内网，是外网到内网的单向通信。

（2）端口映射：将内网IP的某个端口映射到公网IP的某个端口，实现通过公网IP+端口直接正向访问内网，端口映射实现双向通信。

##### 2、隧道技术

是一种通过使用互联网络的基础设施在网络之间传递数据的方式。红队在进攻的时候，需要在复杂的内容中获取稳定的流量交互，以达到目的。攻击者通常会采用各种内网穿透技术建立隧道，从不同网络协议层对出口屏蔽或拦截进行绕过。进行内网渗透常用的隧道技术有dns隧道、http隧道、ssh隧道、icmp隧道等容易被网络防火墙放行的协议。

（1）网络层隧道：IPV6隧道、ICMP隧道、GRE隧道，其中常用的隧道技术是ICMP隧道技术。

（2）传输层隧道：主要有TCP、UDP协议，因此隧道搭建也是基于这两种协议进行的。常用的隧道搭建工具有netcat、powercat等。

（3）应用层隧道：应用层位于TCP/IP协议的最顶层，通常用于搭建各种应用服务，而基于应用层搭建的隧道技术就是利用各种应用所占用的端口进行搭建，比如有SSH、HTTP/HTTPS和DNS服务，这些服务是服务器经常用到不会被禁止的协议。

##### 3、各类工具

如Windows自带的netsh命令、MSF、CS、Neo-reGeorg、proxychains、profiler、ptunnel、icmpsh、PowerCat、netcat、frp、EarthWorm、CobaltStrike等。

#### 二、构建实验环境

##### 1、网络构成：

（1）攻击主机：Kali，IP地址：192.168.1.102 （桥接无线网络）或者 10.10.100.103（桥接电信的有线网络）

![image-20240929170216981](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929170216981.png)

![image-20240929220402704](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929220402704.png)

（2）代理主机：Windows 2016，IP地址：192.168.1.104（桥接无线网络）或者 10.10.100.104（桥接电信的有线网络），10.10.10.128（Host -Only），此环境也可以再准备一台类似的网络配置的CentOS的环境。

![image-20240929170511165](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929170511165.png)

![image-20240929220434631](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929220434631.png)

（3）内网主机：Windows7，IP地址：10.10.10.129 （Host-Only网络）

![image-20240929170714301](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929170714301.png)

![image-20240929220515588](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240929220515588.png)

Kali可以与Windows2016通信，不能与Windows7通信，Windows2016可以与Windows7通信。

![image-20211206022643993](https://gitee.com/ymq_typroa/typroa/raw/main/20211206022644.png)

其中 kali 的 192.168.230.148 在我的实验中是 192.168.1.102

##### 2、修改VMWare网络设置

将VMnet1仅主机模式的IP地址修改为：10.10.10.0，本机默认为192.168.119.0，为了显著区分HostOnly与桥接的IP地址差异而进行的修改，当然，也可以不修改，保持192.168.119.0网段。

![image-20211206014808508](https://gitee.com/ymq_typroa/typroa/raw/main/20211206014808.png)

修改为10.10.10.0/24的目的，是为了让两个网段的IP地址差异更加巨大，当然，不修改也没有问题。

![image-20211206015322564](https://gitee.com/ymq_typroa/typroa/raw/main/20211206015322.png)

为了保持IP地址的固定，也可以手工指定IP地址，而不需要使用Hostonly的DHCP服务。

##### 3、为Windows2016添加新网卡

在VMWare的设置中，为当前虚拟机添加一个新的硬件，选择“网络适配器”。

![image-20211206015540351](https://gitee.com/ymq_typroa/typroa/raw/main/20211206015540.png)

为新增加的网络适配器设置为“仅主机模式”，这样便可以实现与NAT网络的完全隔离，同时与其他仅主机模式的 10.10.10.X 网段内的主机实现通信。人为在VMWare实验环境中构建出来一套内外网隔离环境。

![image-20211206015606175](https://gitee.com/ymq_typroa/typroa/raw/main/20211206015621.png)

打开Windows 2016的命令行，运行ipconfig，查看IP是否修改成功，如果没有成功，可以重启一下网卡。

![image-20211206020511568](https://gitee.com/ymq_typroa/typroa/raw/main/20211206020511.png)

##### 4、修改Windows7的网络设置

将Windows7的网络设置从原本的NAT直接修改为仅主机模式，确保IP地址为10.10.10.X网段，并确认是否可以与 Kali和Windows2016通信，目标为：Windows2016可以与两边互相通信，Windows7无法与Kali通信。(可以使用Ping命令或者访问Web服务)，另外，也可以确认一下，此时Windows7是无法直接访问外部网络的。

![image-20211206022905314](https://gitee.com/ymq_typroa/typroa/raw/main/20211206022905.png)

> 在安装有Python的环境中，如果需要临时启动一个HTTP服务器用于测试，可以直接运行命令：`python -m http.server`

5、配置一台CentOS扮演代理主机的角色，用于Linux相互之间的通信。

6、配置一台云服务器环境，确保有公网IP地址。

![image-20211207051237672](https://gitee.com/ymq_typroa/typroa/raw/main/20211207051237.png)

> 在虚拟机中，由于共用虚拟网卡，所以很有可能存在192.168.112.148可以正常访问10.10.10.129的情况，如果出现这种情况，则将Kali和Win2016修改为桥接模式，完全与Host-Only的Win2016和Win7分开，完成网络设置也是可以的。

